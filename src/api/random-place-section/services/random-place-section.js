'use strict';

/**
 * random-place-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::random-place-section.random-place-section');
