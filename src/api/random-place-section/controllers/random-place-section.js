'use strict';

/**
 * random-place-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::random-place-section.random-place-section');
