'use strict';

/**
 * random-place-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::random-place-section.random-place-section');
